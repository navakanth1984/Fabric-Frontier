# Model Registry

Allows routing to choose not only agents but also foundational models (e.g., GPT, Claude, Gemini, Local LLM).

- `registry.py`: Model profiles and registration.
- `capabilities.py`: Mappings between models and their capabilities.
- `selection_policy.py`: Rules for routing tasks to appropriate models.
