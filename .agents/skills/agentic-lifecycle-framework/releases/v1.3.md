# ACDLC Release Notes - v1.3



Upgraded the platform with a **machine-enforceable policy engine, formal telemetry logs validation, central active registries, and strict circular dependency validation**.



## Ã°Å¸Å¡â‚¬ Enhancements & Core Features

- **JSON Schema Standardizations**: Aligned casing to lowercase primitives (`object`, `string`, `boolean`, `integer`, `number`) inside all state schemas to prevent validation tool errors.

- **Strict Circle Verification Gate**: Upgraded [scripts/verify_dependency_graph.py](../scripts/verify_dependency_graph.py) to exit with code `1` under strict enforcement if circular references are found.

- **Decoupled Graph Ignores**: Moved graph exclusions dynamically into `manifest.yaml` (parsing `ignore_orphans` patterns).

- **Telemetry Event validation**: Created [schemas/telemetry-event.json](../schemas/telemetry-event.json) providing a standard metrics framework.

- **Platform Skill Registry**: Built [registry/manifest.yaml](../registry/manifest.yaml) mapping active system modular frameworks.

- **Enforceable Policies**: Configured the machine-enforceable YAML files in `policies/` controlling token limits, delegation bounds, and budgets.

