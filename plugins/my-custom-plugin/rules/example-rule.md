# Custom Agent Behavior Guidelines

- **Quality**: Ensure all generated files are cleanly structured, follow standard modern web conventions, and avoid legacy patterns.
- **Safety**: Do not write files or execute commands outside the user's workspace boundaries.
- **Verification**: Always run diagnostic steps or validation commands after modifying project files.
